begin
  record Struct
    char array[20] name;
  end record;

  type Struct mine;
  char array[20] other;

  other[0] := '*';
  write other[0], endl;

  mine.name[0] := '$';
  write mine.name[0], endl;
end.
